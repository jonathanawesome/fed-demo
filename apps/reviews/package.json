{
  "name": "@fed-demo/reviews",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "grafbase dev --port 4002",
    "start": "grafbase start",
    "publish:dev": "grafbase publish --dev --name fed-demo-reviews --url http://localhost:4002/graphql"
  },
  "devDependencies": {
    "@grafbase/sdk": "^0.21.0"
  }
}
